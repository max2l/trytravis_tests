# max2l_microservices
max2l microservices repository
## Homework_13__“ехнологи€_контейнеризации__¬ведение_в_Docker
### ¬ процессе сделано
- ”становлен `docker`
- ”становлен `docker-compose`
- «апушено несколько контейнеров
- »зучены основные команды `docker`
###  ак запустить проект:

- Ќиже указными командами можно проверить версию `docker` и его текущее состо€ние. 
```
docker version
docker info
```
- —писок запущенных контейнеров можно посмотреть командой
```
docker ps
```
- —писок скаченных образов можно посмотреть командой
```
docker images
```
- «апустить контейнер из образа 
```
docker run им€_образа
```
- «апустить остановленный контейнер
```
docker start id_контейнера
```
- ѕрисоединитьс€ к запущенному контейнеру
```
docker exec -it id_контейнера bash
docker attach id_контейнера
```
- ƒл€ вывода списока всех контейнеров
```
docker ps -a
```
- ƒл€ удалени€ контейнера или образа 
```
docker rm -f id_контейнера
docker rmi -f id_образа 
```
- —оздать новай образ из существующего контейнера 
```
docker commit id_контейнера им€_образа
```
- ќстановить запущенный контейнер
```
docker stop -f id_контейнера
docker kill -f id_контейнера
```
- ѕосмотреть зан€тое на диске место.
```
docker system df
```
